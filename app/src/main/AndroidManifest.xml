<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nomoretrash">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission-sdk-23 android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:name=".ApplicationDemo"
        android:allowBackup="true"
        android:icon="@mipmap/no_more_trash_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/no_more_trash_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".settings.SettingsActivity"/>
        <activity android:name=".settings.InfoActivity"/>
        <activity android:name=".settings.ConnexionActivity" />
        <activity android:name=".signalements.mes_signalements.MesSignalementsActivity" />
        <activity android:name=".signalements.SignalementActivity" />
        <activity android:name=".statistiques.StatistiquesActivity" />
        <activity android:name=".settings.ContactActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>